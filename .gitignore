# ============================================================
# BtoM —— 反演磁铁内部磁化强度分布
# 只提交「源码 + 原始测量数据 + 文档 + 论文/网站源文件」；
# 一切可重新生成的中间产物、结果文件、图片一律不入库。
#
# 注意：.gitignore 不支持行尾注释（# 只在行首才是注释），
#      所以下面的说明都写在规则上一行。
# ============================================================

# ---------- Python ----------
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# ---------- 编辑器 ----------
.vscode/

# ---------- MkDocs ----------
#
# 站点源码在 web/（markdown 在 web/docs/），构建产物直接写到【仓库根的 docs/】。
# 那份产物是【要提交进仓库】的 —— GitHub Pages 用
#   Settings → Pages → Source: Deploy from a branch → main / docs
# 直接发布它，不需要 gh-pages 分支。
#
# 所以：**千万不要把 docs/ 加进这里**。
#
# site/ 与 web/site/ 是早期用 gh-deploy 时的产物目录，现在不会再生成了；
# 留着只是防止有人在别的目录顺手 mkdocs build 时误提交。
site/
web/site/

# ---------- 反演 / 绘图产物（可由 data/raw + notebooks 重新生成） ----------

# 论文/报告配图，由 notebooks/ 下的脚本生成
figures/

# 网格化后的 vtk/vtu 与数值结果
data/derived/

# meshtest 的实验曲线
meshtest/figures/

# 早期临时试验目录（含 4 MB PDF 与 4×1.7 MB vtu）
archive/test/
